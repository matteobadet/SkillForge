# Env / secrets
.env
.env.*
!.env.example

# .NET
bin/
obj/
*.user
.vs/

# Node / React+Vite
node_modules/
dist/
.vite/
npm-debug.log*

# Docker
docker-compose.override.yml

# Kubernetes secrets (never commit real values — only *.example.yaml)
k8s/**/*.secret.yaml
kubeconfig*
*.kubeconfig

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local Claude settings (not the shared skills/agents)
.claude/settings.local.json
