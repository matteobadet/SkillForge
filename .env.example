# PostgreSQL
POSTGRES_USER=skillforge
POSTGRES_PASSWORD=changeme
POSTGRES_DB=skillforge

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme123
MINIO_BUCKET_AVATARS=avatars
MINIO_BUCKET_RESOURCES=resources
MINIO_BUCKET_ICONS=icons
# Host:port the browser can reach MinIO on (used only to sign avatar URLs)
MINIO_PUBLIC_ENDPOINT=localhost:9000

# JWT (generate a long random string for production)
JWT_SIGNING_KEY=dev-only-signing-key-change-me-please-0123456789
JWT_ISSUER=skillforge

# Ports exposed on the host
API_PORT=5080
FRONTEND_PORT=5173
